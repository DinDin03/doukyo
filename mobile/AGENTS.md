# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project is pinned to **Expo SDK 54** to match the Expo Go client (54.x) on the test device.
Do not upgrade the SDK without re-checking the installed Expo Go version.
